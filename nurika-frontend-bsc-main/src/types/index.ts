export enum GAS_PRICE {
  default = "5",
  fast = "6",
  instant = "7",
  testnet = "10",
}

export interface Address {
  97: string;
  56: string;
}

export enum ChainId {
  MAINNET = 56,
  TESTNET = 97,
}
